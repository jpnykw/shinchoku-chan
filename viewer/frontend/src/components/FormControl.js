import { default as MaterialFormControl } from '@material-ui/core/FormControl';

const FormControl = (props) => {
  return (
    <MaterialFormControl {...props} />
  );
};

export default FormControl;