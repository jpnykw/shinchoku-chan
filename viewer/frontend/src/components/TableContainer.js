import { default as MaterialTableContainer } from '@material-ui/core/TableContainer';

const TableContainer = (props) => {
  return (
    <MaterialTableContainer {...props} />
  );
};

export default TableContainer;